/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.leadconnectorhq.com', 'media.gettyimages.com'],
  },
};

export default nextConfig;
